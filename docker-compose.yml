version: '3.8'

services:
  vault:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - vault-data:/app/server/data
    environment:
      - PORT=4000
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  vault-data:
